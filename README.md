# Smartdesk Core

This repository contains:

1. Common Java library for microservices
2. Docker Compose setup for local development

## Build Library

cd common-lib
mvn clean install

## Start Infrastructure

cd docker
docker-compose up -d
